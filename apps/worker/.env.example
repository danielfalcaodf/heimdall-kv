# .env.example — Worker (NestJS)
# As variáveis principais estão centralizadas na raiz do monorepo.
# Copie o .env.example da raiz: cp .env.example .env
#
# Este arquivo sobrescreve apenas a porta do processo do worker.

PORT=3002
