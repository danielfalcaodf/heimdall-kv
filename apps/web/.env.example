# .env.example — Web (Next.js)
# Copie para .env.local e preencha os valores reais (nunca versionar .env.local)

# URL da API backend
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_DEMO_ROLE=administrator

# Ambiente
NODE_ENV=development
